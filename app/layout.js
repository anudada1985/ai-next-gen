export const metadata = {
  title: "AI NextGen",
  description: "Empowering Students with Intelligent Skills",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
